# Supabase project settings → API.
# The anon key is the only key this app needs: authorization lives entirely in
# the row level security policies in supabase/migrations/0001_schema.sql.
# Never put the service_role key in here.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-public-key
